//----------------------------------------------------------------------------
#include <gtest/gtest.h>
#include "Fmt.h"
//----------------------------------------------------------------------------
TEST(FmtTest, Default)
{
    kw::Fmt fmt("str:%s", "test");
    EXPECT_STREQ(fmt.data(), "str:test");
    EXPECT_EQ(fmt.size(), strlen("str:test"));
}
//----------------------------------------------------------------------------
